# Product Roadmap — Tranqui

> Generated by PLAID. Checkboxes are updated as tasks are completed.
> The coding agent MUST mark tasks `- [x]` as they are finished.

**Status:** 25/87 tasks complete
**Current Phase:** Phase 1 — Pet Profiles & Household Foundation

## Build Philosophy

1. **Each phase ships something usable.** No phase leaves the app broken. After any phase, you can demo what you've built.
2. **Infrastructure before features.** Phase 0 sets up everything so feature work in later phases is fast and clean.
3. **Magic moment first.** The "ahorro de 100€ y noche tranquila" is achievable by the end of Phase 2. Everything afterward builds on top.
4. **Test as you go.** Don't save testing for the end. Each task includes a verification step.
5. **Progressive enhancement.** Start with the simplest working version, then layer on polish.
6. **Review before proceeding.** Push each completed phase as a PR and let a review agent (e.g. CodeRabbit) review it before starting the next phase. External review catches issues the coding agent will not flag.
7. **Clinical safety is a hard gate.** No phase that touches clinical reasoning ships without verification of the disclaimer surface, the lenguaje probabilístico, and the URGENTE bypass of the paywall. These are P0 product principles, not nice-to-haves.

-----

## Phase 0: Foundation & Setup

> **Goal:** A running Next.js app deployed to Vercel with Convex, Clerk, design tokens, and the disclaimer constant in place. No features yet — only the shell, the wiring, and a "Hello, Tranqui" route that proves auth + data round-trips work.

**Reference sections — read these before starting this phase:**
- PRD: § 2. Technical Architecture, § 9. Design System, § 10. Auth Implementation
- Vision: § 5. Design Direction > Color Palette, Typography, Spacing & Layout, Design Tokens

> **Phase prompt — give this to your coding agent:**
> *"Read docs/product-roadmap.md and find Phase 0. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-0/foundation-and-setup`, commit, push, and open a PR for review."*

- [x] **TASK-001** — Initialize the Next.js project with TypeScript, Tailwind, App Router and `src/` directory.
  Files: `package.json`, `tsconfig.json`, `next.config.mjs`, `tailwind.config.ts`, `postcss.config.mjs`, `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/globals.css`.
  Notes: Run `pnpm create next-app@latest tranqui --typescript --tailwind --app --src-dir --import-alias "@/*"`. Replace the default home page with a centered "Hola, Tranqui" placeholder. Verify: `pnpm dev` and the page renders at http://localhost:3000.

- [x] **TASK-002** — Add design tokens to `globals.css` and the Tailwind config.
  Files: `src/app/globals.css`, `tailwind.config.ts`.
  Notes: Copy the CSS variables from `docs/prd.md § 9 → Color Tokens, Typography Tokens, Spacing Tokens` into `globals.css`. Extend `tailwind.config.ts` with the cream/sage/coral palette, fontFamily, borderRadius and boxShadow per `docs/prd.md § 9 → Tailwind Configuration`. Import Inter, Fraunces and JetBrains Mono via Google Fonts in `globals.css`. Verify: temporary debug page renders text in `font-sans` color `--color-text-primary` on background `--color-bg-base`.

- [x] **TASK-003** — Add Lucide icons, `clsx` + `tailwind-merge` utility, and a base `cn()` helper.
  Files: `src/lib/utils.ts`.
  Notes: `pnpm add lucide-react clsx tailwind-merge class-variance-authority`. Export `cn(...inputs) => twMerge(clsx(inputs))`. Verify: import a Lucide icon in the home page; it renders.

- [x] **TASK-004** — Initialize Convex and link to a development deployment.
  Files: `convex/_generated/`, `convex/auth.config.ts` (placeholder), `.env.local`.
  Notes: `pnpm dlx convex@latest dev --once` then keep `pnpm dlx convex@latest dev` running. Save `NEXT_PUBLIC_CONVEX_URL` to `.env.local`. Verify: convex CLI confirms the dev deployment is provisioned.

- [x] **TASK-005** — Add the Convex React provider wired through Clerk.
  Files: `src/components/shared/ConvexClientProvider.tsx`, `src/app/layout.tsx`.
  Notes: `pnpm add convex @clerk/nextjs @clerk/themes @clerk/localizations`. Implement `ConvexClientProvider` per `docs/prd.md § 10 → Provider Configuration`. Wrap `<RootLayout>` in `<ClerkProvider localization={esES}>` then `<ConvexClientProvider>`. Set `<html lang="es-ES">`. Verify: app still loads without errors.

- [x] **TASK-006** — Configure Clerk and add middleware to protect `/(app)/`, `/(vet)/`, `/settings`.
  Files: `src/middleware.ts`, `.env.local`.
  Notes: Create a Clerk app at clerk.com; copy `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY` to `.env.local`. Implement `middleware.ts` per `docs/prd.md § 10 → Provider Configuration`. Verify: visiting `/settings` while signed-out redirects to Clerk's sign-in page.

- [x] **TASK-007** — Wire Convex to accept Clerk JWTs.
  Files: `convex/auth.config.ts`, `.env.local`, Convex env via `npx convex env set`.
  Notes: In Clerk dashboard, create a JWT template named `convex` (template = "Convex"). Copy the `Issuer` URL into `CLERK_JWT_ISSUER_DOMAIN`. Implement `convex/auth.config.ts` per `docs/prd.md § 10 → Provider Configuration`. Set `CLERK_JWT_ISSUER_DOMAIN` in Convex env (`npx convex env set CLERK_JWT_ISSUER_DOMAIN <url>`). Verify: a debug Convex query that returns `ctx.auth.getUserIdentity()` returns the user when called from a signed-in client.

- [x] **TASK-008** — Enable `@convex-dev/eslint-plugin` and configure ESLint + Prettier.
  Files: `eslint.config.js`, `.prettierrc`, `package.json`.
  Notes: `pnpm add -D @convex-dev/eslint-plugin eslint prettier prettier-plugin-tailwindcss`. Implement the recommended config per `skills/convex/convex-best-practices/SKILL.md`. Add scripts: `"lint": "next lint"`, `"format": "prettier -w ."`, `"typecheck": "tsc --noEmit"`. Verify: `pnpm lint` runs without errors on the empty project.

- [x] **TASK-009** — Create the marketing route group with a placeholder landing page.
  Files: `src/app/(marketing)/layout.tsx`, `src/app/(marketing)/page.tsx`.
  Notes: Move the home content into `(marketing)/page.tsx`. The layout for `(marketing)` is minimal (no nav). The landing page renders just `<h1 className="font-display text-4xl">Tranqui</h1>` and `<p>Próximamente</p>` for now — the full landing comes in Phase 5.

- [x] **TASK-010** — Create the authenticated app shell `(app)/layout.tsx` with a top nav.
  Files: `src/app/(app)/layout.tsx`, `src/components/shared/AppNav.tsx`.
  Notes: Top nav contains the Tranqui wordmark, links to "Mascotas", "Historial", "Reservas", "Ajustes", and Clerk's `<UserButton>`. Use sage/cream colors. Verify: visiting `/pets` while signed in shows the nav.

- [x] **TASK-011** — Add the disclaimer text constant module.
  Files: `src/lib/disclaimer.ts`.
  Notes: Export `DISCLAIMER_TEXT_SHORT` (≤2 lines, used in chat banner and analysis footer) and `DISCLAIMER_TEXT_LONG` (used in `/legal/disclaimer`). Wording: "Soy un asistente de IA con conocimiento veterinario de referencia. La información que ofrezco es orientativa y no sustituye el diagnóstico de un veterinario colegiado. Ante cualquier signo de urgencia, acude inmediatamente a una clínica veterinaria." Verify: import the constant from a test page.

- [x] **TASK-012** — Build the core UI primitives: `Button`, `Input`, `Textarea`, `Card`, `Badge`.
  Files: `src/components/ui/Button.tsx`, `Input.tsx`, `Textarea.tsx`, `Card.tsx`, `Badge.tsx`, `src/components/ui/index.ts`.
  Notes: Follow `docs/prd.md § 9 → Component Specifications` for variants, sizes, states. Use `class-variance-authority` for variants. Verify: a temporary `/debug` page renders all variants/sizes and looks coherent.

- [x] **TASK-013** — Build `TriageBadge` and `DisclaimerBanner` components.
  Files: `src/components/chat/TriageBadge.tsx`, `src/components/chat/DisclaimerBanner.tsx`.
  Notes: TriageBadge accepts `level: "urgente" | "preferente" | "orientativo"` and renders the right semantic color/copy per `docs/prd.md § 9 → Component Specifications → TriageBadge`. DisclaimerBanner pulls from `DISCLAIMER_TEXT_SHORT`. Verify: render all three TriageBadge levels on `/debug`.

- [x] **TASK-014** — Add deployment configuration to Vercel (production environment).
  Files: `vercel.json` (optional), README updates.
  Notes: Connect the GitHub repo to Vercel. Add envs in Vercel: `NEXT_PUBLIC_CONVEX_URL`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`. Add `npx convex deploy --cmd 'pnpm build'` as the build command for production. Verify: trigger a preview deploy on a feature branch and confirm it builds and runs.

- [x] **TASK-015** — Add the static clinical SKILL into the project and a loader.
  Files: `skills/agente-veterinario-ia.md`, `convex/lib/clinicalSkill.ts`.
  Notes: Confirm `skills/agente-veterinario-ia.md` is committed (it should already be in the project root from the workspace). Implement `clinicalSkill.ts` that exports the SKILL content as a single string. The simplest implementation reads the file at build time via `import skillRaw from "../../skills/agente-veterinario-ia.md?raw"` (configure raw imports if needed) or copies the content into a `.ts` file generated by a script. Verify: a debug Convex query returns the first 200 chars of the SKILL.

- [x] **TASK-016** — Push Phase 0 as a PR.
  Notes: `git checkout -b phase-0/foundation-and-setup`, commit all the above, push, open a PR titled "Phase 0: Foundation & Setup". Body includes phase goal and task count. Wait for review (e.g. CodeRabbit) before starting Phase 1.

-----

## Phase 1: Pet Profiles & Household Foundation

> **Goal:** Authenticated users can sign up, an associated household and user record auto-create, and they can add/edit/list pets in their household via reactive UI.

**Reference sections:**
- PRD: § 3. Data Model (users, households, pets), § 4. API Specification (users, pets), § 6. Functional Requirements > FR-002, FR-019, FR-020, § 8. UI/UX Requirements > Pets List, Pet Detail / Edit
- Vision: § 5. Design Direction > Component Philosophy

> **Phase prompt:**
> *"Read docs/product-roadmap.md and find Phase 1. Read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. Mark each task complete after finishing it. After all tasks, push as `phase-1/pet-profiles-and-household` and open a PR for review."*

- [x] **TASK-017** — Define the initial Convex schema with `users`, `households`, `pets`.
  Files: `convex/schema.ts`.
  Notes: Implement only the three tables and their indexes per `docs/prd.md § 3 → Entity Definitions`. Run `pnpm dlx convex@latest dev` and confirm the schema compiles. Verify: tables visible in Convex dashboard.

- [x] **TASK-018** — Implement `users.bootstrap` mutation (idempotent user + household creation).
  Files: `convex/users.ts`.
  Notes: On first call, read `ctx.auth.getUserIdentity()`; if no `users` row exists for `clerkId`, create one with `role: "owner"`, then create a default household named after the user, set `householdId`. Return `users._id`. Verify with a Convex test that calling twice produces the same `_id`.

- [x] **TASK-019** — Implement `users.me` query that returns the current user + household + (optional) subscription stub.
  Files: `convex/users.ts`.
  Notes: For now, the subscription field returns `null`. Validate args with `args: {}`, validate return shape per the PRD spec.

- [x] **TASK-020** — Implement `pets.list`, `pets.get`, `pets.create`, `pets.update`, `pets.remove`.
  Files: `convex/pets.ts`.
  Notes: Every function verifies ownership: pet's `householdId` must match the caller's `householdId`. `pets.remove` is a soft delete (clears `householdId` to null and adds a `deletedAt` field — extend the schema if needed). Use `v.id("pets")` for IDs.

- [x] **TASK-021** — Build the bootstrap-on-mount client hook.
  Files: `src/components/shared/UserBootstrap.tsx`, `src/app/(app)/layout.tsx`.
  Notes: Component uses `useMutation(api.users.bootstrap)` once on first mount inside the authenticated shell. Render in `(app)/layout.tsx`. Verify: signing up a fresh user creates a row in `users` and a row in `households`.

- [x] **TASK-022** — Build the Pets list page `/pets` (empty + populated states).
  Files: `src/app/(app)/pets/page.tsx`, `src/components/pets/PetCard.tsx`, `src/components/shared/EmptyState.tsx`.
  Notes: Use `usePaginatedQuery` if list grows; for MVP, plain `useQuery(api.pets.list)` is fine. Empty state with a friendly illustration placeholder + "Añadir mascota" CTA. Populated grid responsive 1/2/3 cols per design tokens.

- [x] **TASK-023** — Build the Pet form (create + edit) used by `/pets/new` and `/pets/[petId]`.
  Files: `src/app/(app)/pets/new/page.tsx`, `src/app/(app)/pets/[petId]/page.tsx`, `src/components/pets/PetForm.tsx`.
  Notes: Use `react-hook-form + zod`. Schema: `name (required, ≤40 chars)`, `species ("dog"|"cat", required)`, `breed`, `sex`, `neutered`, `birthYear`, `weightKg`, `knownAllergies`. Submit calls `pets.create` or `pets.update`. On success, navigate back to `/pets`. Verify: create, edit and delete a pet end-to-end.

- [x] **TASK-024** — Add a placeholder Settings page `/settings` with profile fields and household name editing.
  Files: `src/app/(app)/settings/page.tsx`, `src/components/shared/SettingsForm.tsx`, `convex/users.ts` (extend with `users.updateProfile` and `households.rename`).
  Notes: Implement FR-019 minimally: name, locale, household name. Verify: changes persist and the nav reflects the new name.

- [x] **TASK-025** — Push Phase 1 as a PR.
  Notes: Branch `phase-1/pet-profiles-and-household`. Title "Phase 1: Pet Profiles & Household Foundation". Wait for review.

-----

## Phase 2: Core Consultation Experience (Magic Moment)

> **Goal:** The magic moment lives. A user (signed-in or in free trial) can describe their pet's symptoms, see a streamed assistant response with directed anamnesis, and receive a structured triage analysis with the disclaimer always visible. URGENTE classification bypasses any paywall.

**Reference sections:**
- PRD: § 1. Overview > Magic Moment, § 3. Data Model (consultations, messages, freeTrialUsage), § 4. API Specification (consultations, messages, ai), § 6. Functional Requirements > FR-001, FR-003, FR-004, FR-005, FR-011, FR-012, FR-015, FR-016, § 8. UI/UX Requirements > Free-trial Consultation Entry, Active Consultation, AnalysisCard, History, § 12. Edge Cases & Error Handling > Consultation chat
- Vision: § 3. Product Strategy > Magic Moment Design, § 4. Brand Strategy > Voice & Tone Guide

> **Phase prompt:**
> *"Read docs/product-roadmap.md and find Phase 2. Read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. The magic moment must work end-to-end by the end of this phase. Mark each task complete after finishing it. After all tasks, push as `phase-2/core-consultation` and open a PR for review."*

- [ ] **TASK-026** — Extend the schema with `consultations`, `messages`, `freeTrialUsage`.
  Files: `convex/schema.ts`.
  Notes: Add the three tables exactly as defined in `docs/prd.md § 3 → Entity Definitions`. Add all the indexes listed in § 3 → Indexes for these tables.

- [ ] **TASK-027** — Implement `consultations.start` mutation (authenticated path).
  Files: `convex/consultations.ts`.
  Notes: Validate ownership of `petId`. Create a `consultations` row with `status: "anamnesis"`. Insert the first user `messages` row. Schedule `ai.continueConversation` via `ctx.scheduler.runAfter(0, internal.ai.continueConversation, { consultationId })`. Return the `consultationId`.

- [ ] **TASK-028** — Implement `consultations.startFreeTrial` mutation (no auth).
  Files: `convex/consultations.ts`, `convex/freeTrialUsage.ts`.
  Notes: Per FR-001. Use a sentinel "anonymous" household — implementation suggestion: keep the consultation rows tied to `email` only and use `v.optional(v.id("households"))` (extend schema accordingly, or create a single dedicated anonymous household and reuse it). Enforce 3-per-email-per-calendar-month limit using `freeTrialUsage`. Return `{ consultationId, isLimitReached }`.

- [ ] **TASK-029** — Implement `consultations.list`, `consultations.get`, `consultations.close`.
  Files: `convex/consultations.ts`.
  Notes: `list` uses `paginationOptsValidator` and `by_household_and_created` index; supports optional `petId` filter. `get` denormalizes the pet. `close` sets `status: "closed"` and `closedAt`. Validate ownership.

- [ ] **TASK-030** — Implement `messages.listByConsultation` query and `messages.appendUserMessage` mutation.
  Files: `convex/messages.ts`.
  Notes: `listByConsultation` uses `by_consultation` index. `appendUserMessage` validates ownership, appends a user message and schedules `ai.continueConversation`. Both functions use `v.id` validators.

- [ ] **TASK-031** — Implement `internalMutation` `messages.appendAssistantDelta` and `messages.finalizeAssistantMessage`.
  Files: `convex/messages.ts`.
  Notes: `appendAssistantDelta` concatenates `delta` to existing `content`. `finalizeAssistantMessage` sets `isStreaming: false`, optionally sets `structuredAnalysis`, and updates the parent `consultations` row's `triageLevel` and `status: "analyzed"` if final. Use `internalMutation` from `_generated/server` so it cannot be called from the client.

- [ ] **TASK-032** — Implement the prompt assembly module.
  Files: `convex/lib/prompt.ts`.
  Notes: Function `buildSystemPrompt({ pet, recentConsultations })` returns the system prompt string composed of: (a) the SKILL content from `clinicalSkill.ts`, (b) a "Pet context" section with the pet's known fields, (c) a "Recent history" section summarising up to 3 prior consultations (titles + triage levels + dates), (d) the structured-output instruction (final analysis as a JSON block fenced with `<analysis>...</analysis>`), (e) the disclaimer reminder. The structured-output instruction should ask the model to output a JSON object matching the `structuredAnalysis` shape declared in the schema, plus `summaryTitle`. Keep the SKILL portion at the top so prompt caching can be applied.

- [ ] **TASK-033** — Implement the `ai.continueConversation` action with Anthropic streaming.
  Files: `convex/ai.ts`, `package.json`.
  Notes: `pnpm add @anthropic-ai/sdk`. Set `ANTHROPIC_API_KEY` in Convex env. The action: fetches consultation, pet, last N messages via internal queries; assembles the system prompt; creates an empty assistant `messages` row with `isStreaming: true`; streams via `client.messages.stream` with `model: "claude-sonnet-4-..."` (use the latest available Sonnet); buffers deltas and calls `appendAssistantDelta` every ~200ms or every ~50 tokens; on stream end, parses any `<analysis>...</analysis>` JSON block and calls `finalizeAssistantMessage` accordingly; logs token usage and cost on the consultation row (FR-015). Mark with `"use node"` directive at the top of the file. Verify: a triggered consultation streams to the DB and the chat UI updates live.

- [ ] **TASK-034** — Build the chat UI components.
  Files: `src/components/chat/MessageList.tsx`, `MessageBubble.tsx`, `StreamingCursor.tsx`, `ChatInput.tsx`.
  Notes: `MessageList` subscribes to `messages.listByConsultation` and renders ordered turns. `MessageBubble` renders user/assistant variants per `docs/prd.md § 9`. `StreamingCursor` is a small pulsing dot rendered at the end of an assistant message while `isStreaming`. `ChatInput` is a textarea + send button with submit-on-Enter (Shift+Enter for newline). Add `aria-live="polite"` on the assistant streaming message.

- [ ] **TASK-035** — Build the `AnalysisCard` component.
  Files: `src/components/chat/AnalysisCard.tsx`, `src/components/chat/ProbableCausesList.tsx`, `src/components/chat/ActionsList.tsx`.
  Notes: Renders the structured analysis per `docs/prd.md § 8 → Screen: AnalysisCard component`. Top: TriageBadge with semantic color and label. Sections: probable causes, recommended actions, observation guidance. Footer: disclaimer. Primary CTA: "Reservar veterinario" — but in this phase the CTA is rendered as disabled with tooltip "Disponible en Fase 3", so as not to break the flow.

- [ ] **TASK-036** — Build the active consultation page `/consult/[consultationId]`.
  Files: `src/app/(app)/consult/[consultationId]/page.tsx`, `src/components/chat/ChatHeader.tsx`.
  Notes: Renders chat header (pet name + species icon + back button), DisclaimerBanner pinned, MessageList, ChatInput. Disable input while last message is streaming. When the latest assistant message has `isFinalAnalysis: true`, render the AnalysisCard inline.

- [ ] **TASK-037** — Build the consultation entry page `/consult` for signed-in users.
  Files: `src/app/(app)/consult/page.tsx`.
  Notes: Pet selector (or "Add pet" CTA if no pets). Single textarea ("¿Qué te preocupa hoy?") + send. On submit, calls `consultations.start` and navigates to `/consult/[id]`.

- [ ] **TASK-038** — Build the free-trial consultation entry page `/consult/new` (unauth-friendly).
  Files: `src/app/(app)/consult/new/page.tsx` (route group note: this lives outside the `(app)` group — implement it under `src/app/consult/new/page.tsx` so it's public).
  Notes: Two-step wizard per `docs/prd.md § 8 → Free-trial Consultation Entry`. Step 1: email + name + species + optional age. Step 2: question textarea. Submit calls `consultations.startFreeTrial`. On success, redirect to a public `/consult/[id]` route that does NOT require auth (gate by URL token + email match server-side). Show paywall if `isLimitReached: true` with "Crear cuenta" CTA.

- [ ] **TASK-039** — Add `@convex-dev/rate-limiter` and apply rate limits.
  Files: `convex/rateLimit.ts`, integrations in `consultations.ts`, `messages.ts`.
  Notes: `pnpm add @convex-dev/rate-limiter`. Configure: free-trial entry (3 / email / 30 days), AI message rate (30 / user / minute), booking creation placeholder for Phase 3 (5 / user / day). Per FR-016. Verify: send 31 messages in a minute and confirm a friendly toast appears on the 31st.

- [ ] **TASK-040** — Build the History page `/history`.
  Files: `src/app/(app)/history/page.tsx`, `src/components/chat/ConsultationListItem.tsx`.
  Notes: Per `docs/prd.md § 8 → History` and FR-011. Use `usePaginatedQuery(api.consultations.list, { paginationOpts, petId })`. Pet filter dropdown. Empty state with CTA. Verify: a user with 3 consultations sees them sorted reverse-chronologically.

- [ ] **TASK-041** — Implement URGENTE bypass logic across UI and API.
  Files: `src/components/chat/AnalysisCard.tsx`, `src/app/(app)/consult/new/page.tsx`, `convex/consultations.ts`.
  Notes: When the analysis is URGENTE, the analysis is shown in full without paywall regardless of free-tier limit. Add an "Urgencias 24/7 cerca" link (Google Maps query `https://www.google.com/maps/search/veterinario+urgencias+24h` opened in a new tab). Per § 6 → FR-004 and Open Question Q4. Verify: simulate a free-tier user past the limit getting an URGENTE analysis — paywall does not show.

- [ ] **TASK-042** — Add basic error handling to the consultation flow.
  Files: `src/components/chat/MessageList.tsx`, `convex/ai.ts`.
  Notes: Per § 12 → Consultation chat: if Anthropic action throws, the assistant message stays with `isStreaming: false` and `content: "Tranqui está descansando un momento..."` plus a "Reintentar" button that schedules the action again. Do not deduct from free-tier counter on failure.

- [ ] **TASK-043** — Sample-prompt audit pass with the clinical advisor.
  Notes: Run 10 representative consultations (5 dog, 5 cat — at least 1 URGENTE, 2 PREFERENTE, 7 ORIENTATIVO) end-to-end and review the outputs with the clinical advisor (the founder's partner). Document any issues in `docs/prompt-audit.md` (create the file). Iterate the SKILL or the prompt until ≥9/10 produce appropriate triage. This is a quality gate — do not proceed without passing. Verify: `docs/prompt-audit.md` records the run and approval.

- [ ] **TASK-044** — Push Phase 2 as a PR.
  Notes: Branch `phase-2/core-consultation`. Title "Phase 2: Core Consultation Experience". Wait for review.

-----

## Phase 3: Veterinarian Booking & Video Consultation

> **Goal:** A consultation classified PREFERENTE or URGENTE leads to a real bookable 20-min slot with an active partnered vet, automated email confirmations, and a working in-browser video room. Vets have a basic dashboard to manage availability and join calls.

**Reference sections:**
- PRD: § 3. Data Model (veterinarians, availabilitySlots, bookings), § 4. API Specification (vets, availability, bookings), § 6. Functional Requirements > FR-006, FR-007, FR-014, FR-017, § 8. UI/UX Requirements > Bookings — New, Booking Detail, Vet Dashboard, Vet — Active Consultation, § 12. Edge Cases & Error Handling > Booking
- Vision: § 2. User Research > Secondary Personas (Veterinario aliado), § 3. Product Strategy > Product Principles (vet humano gana visibilidad)

> **Phase prompt:**
> *"Read docs/product-roadmap.md and find Phase 3. Read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. Mark each task complete. After all tasks, push as `phase-3/vet-booking-and-video` and open a PR for review."*

- [ ] **TASK-045** — Extend schema with `veterinarians`, `availabilitySlots`, `bookings`.
  Files: `convex/schema.ts`.
  Notes: Per `docs/prd.md § 3 → Entity Definitions`. Add all listed indexes.

- [ ] **TASK-046** — Implement vet user flow: a `vets.upgradeToVet` admin-only mutation that promotes a `users.role` from "owner" to "vet" and creates the `veterinarians` row.
  Files: `convex/vets.ts`, `convex/lib/permissions.ts`.
  Notes: Add a permissions helper that checks `role === "admin"` (admin role is set manually on Manuel's user via Convex dashboard for MVP). Mutation accepts `userId`, `fullName`, `licenseNumber`, `specialty`, `timezone: "Europe/Madrid"`, `revenueShareBps: 6000`. Sets `isActive: true`.

- [ ] **TASK-047** — Implement `vets.listActive`, `availability.openSlot`, `availability.removeSlot`, `availability.listOpenSlots`.
  Files: `convex/availability.ts`, `convex/vets.ts`.
  Notes: `listActive` returns active vets with name, photo, specialty, bio. `openSlot` is vet-only and rejects past or overlapping slots. `removeSlot` is vet-only and rejects if the slot is `booked`. `listOpenSlots(fromTs, toTs)` queries the `by_status_and_start` index and joins vet info.

- [ ] **TASK-048** — Implement `bookings.create` (excluding payment integration — defer to Phase 4).
  Files: `convex/bookings.ts`.
  Notes: For now, `paymentMode: "included_in_subscription"` always succeeds (no entitlement check yet). Atomically: lock the slot (mark `booked` and `bookingId`), create booking row, schedule `dailyRooms.createForBooking`, schedule `email.sendBookingConfirmation`. Per FR-006. Use `ctx.scheduler` for the side effects so the mutation stays atomic.

- [ ] **TASK-049** — Implement Daily.co room creation action.
  Files: `convex/dailyRooms.ts`, `package.json`.
  Notes: `pnpm add @daily-co/daily-js @daily-co/daily-react`. Set `DAILY_API_KEY` in Convex env. The action POSTs to Daily's REST API to create a one-off room with privacy "public" (or "private" if available on free tier), expiry = booking end + 30 min, and stores the URL on the `bookings` row. Mark with `"use node"`. Per `docs/prd.md § 12 → Booking → Daily room creation fails`: if the API errors, schedule a retry every 5 min; if still failing 1h before start, fall back to a manually-generated Whereby URL (admin notified).

- [ ] **TASK-050** — Implement Resend transactional email actions.
  Files: `convex/email.ts`, `src/emails/BookingConfirmed.tsx`, `BookingReminder.tsx`, `BookingCompleted.tsx`, `package.json`.
  Notes: `pnpm add resend react-email @react-email/components`. Set `RESEND_API_KEY`. Create three React Email templates per `docs/prd.md § 6 → FR-017`. Action `email.sendBookingConfirmation({ bookingId })` fetches booking, vet, pet, renders the React Email, sends via Resend. Schedule `email.sendBookingReminder` to run 15 min before the booking start (`ctx.scheduler.runAt`). Verify: a test booking sends the confirmation to the owner's inbox.

- [ ] **TASK-051** — Build the bookings creation flow `/bookings/new?consultationId=...`.
  Files: `src/app/(app)/bookings/new/page.tsx`, `src/components/bookings/CalendarGrid.tsx`, `src/components/bookings/VetCarousel.tsx`, `src/components/bookings/ConfirmModal.tsx`.
  Notes: Per `docs/prd.md § 8 → Bookings — New`. Calendar covers next 7 days, 30-min granularity. Click a slot → confirm modal. Confirm → `bookings.create` → navigate to `/bookings/[id]`. Handle `code: "SLOT_TAKEN"` with a toast + auto-refresh.

- [ ] **TASK-052** — Wire the AnalysisCard CTA "Reservar veterinario" to `/bookings/new`.
  Files: `src/components/chat/AnalysisCard.tsx`.
  Notes: Replace the disabled stub from Phase 2. Pass `consultationId` via query param.

- [ ] **TASK-053** — Build the booking detail page `/bookings/[bookingId]` with pre-call lobby and in-call view.
  Files: `src/app/(app)/bookings/[bookingId]/page.tsx`, `src/components/bookings/PreCallLobby.tsx`, `src/components/bookings/InCallView.tsx`, `src/components/bookings/PostCallSummary.tsx`.
  Notes: Per `docs/prd.md § 8 → Booking Detail`. Use `@daily-co/daily-react`'s `<DailyProvider>` and `<DailyVideo>` components. Time-driven mode switching (use `Date.now()` and the booking's `scheduledStartAt/endAt`).

- [ ] **TASK-054** — Build the bookings list page `/bookings`.
  Files: `src/app/(app)/bookings/page.tsx`, `src/components/bookings/BookingListItem.tsx`.
  Notes: List of upcoming + past bookings. Each row: pet, vet, time, status badge. Click → detail.

- [ ] **TASK-055** — Build the vet dashboard `/(vet)/dashboard` with tabs for calendar and bookings.
  Files: `src/app/(vet)/layout.tsx`, `src/app/(vet)/dashboard/page.tsx`, `src/components/vet/CalendarEditor.tsx`, `src/components/vet/VetBookingsList.tsx`.
  Notes: Layout enforces `role === "vet"` (server-checked via `users.me`). Calendar tab: week view of own slots; click empty cells to open new slot, click own booked slots to see booking. Bookings tab: list of upcoming bookings with "Entrar a videoconsulta" button visible 10 min before start.

- [ ] **TASK-056** — Build the vet's active-consultation page `/(vet)/consultation/[bookingId]`.
  Files: `src/app/(vet)/consultation/[bookingId]/page.tsx`.
  Notes: Per `docs/prd.md § 8 → Vet — Active Consultation`. Two-column layout. Notes editor saves to `bookings.vetNotesAfter` via `bookings.markCompleted`. On marking completed, schedule `email.sendBookingCompletedToOwner`.

- [ ] **TASK-057** — Implement `bookings.cancelByUser` and `bookings.cancelByVet`.
  Files: `convex/bookings.ts`.
  Notes: Per `docs/prd.md § 12 → Booking`. Free the slot (mark `open`), update booking status. If vet cancels < 2h before start: send apology email + auto-refund flag (refund executed manually for MVP).

- [ ] **TASK-058** — Onboard the first vet (clinical advisor — Manuel's partner) and run a real booking end-to-end.
  Notes: Manually call `vets.upgradeToVet` for the partner's user. Open 4 slots for testing. Run a full booking as a test owner: from a consultation, book a slot, both join the Daily room, the vet takes notes, marks completed, the owner receives the email. Document any issues in `docs/launch-readiness.md`. This is a quality gate — fix any blockers before continuing.

- [ ] **TASK-059** — Push Phase 3 as a PR.
  Notes: Branch `phase-3/vet-booking-and-video`. Title "Phase 3: Veterinarian Booking & Video Consultation". Wait for review.

-----

## Phase 4: Subscription & Billing

> **Goal:** Users can subscribe to the €9.99/mo plan via Polar Checkout, the included monthly consultation gates booking properly, extra €25 video consultations work via one-off Polar orders, and webhooks reliably keep subscription state in sync. Cancellation is one-screen.

**Reference sections:**
- PRD: § 3. Data Model (subscriptions), § 4. API Specification (subscriptions, polarWebhook), § 6. Functional Requirements > FR-008, FR-009, FR-010, § 8. UI/UX Requirements > Settings / Billing, § 11. Payment Integration, § 12. Edge Cases & Error Handling > Subscription
- Vision: § 3. Product Strategy > Product Principles (urgencia rompe el paywall, dark patterns)

> **Phase prompt:**
> *"Read docs/product-roadmap.md and find Phase 4. Read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. Mark each task complete. After all tasks, push as `phase-4/subscription-and-billing` and open a PR for review."*

- [ ] **TASK-060** — Complete Polar merchant onboarding and create the products (out-of-band).
  Notes: Sign up at polar.sh, complete the merchant verification (Spain). Create three products per `docs/prd.md § 11 → Provider Setup`: monthly subscription €9.99, yearly €99 (optional in MVP), one-time €25 extra consultation. Save product IDs to `.env.local` (e.g. `NEXT_PUBLIC_POLAR_PRODUCT_ID_MONTHLY`). Save `POLAR_ACCESS_TOKEN` and `POLAR_WEBHOOK_SECRET` to Convex env. This is a blocking step.

- [ ] **TASK-061** — Extend schema with `subscriptions` and `webhookEvents`.
  Files: `convex/schema.ts`.
  Notes: Implement `subscriptions` per § 3. Add `webhookEvents` (auxiliary table for idempotency: `eventId: v.string()` with unique index `by_event_id`). Field `consultationsUsedThisPeriod` defaults to 0; `includedConsultationsPerPeriod` defaults to 1.

- [ ] **TASK-062** — Implement `subscriptions.startCheckout` mutation/action and `subscriptions.mine` query.
  Files: `convex/subscriptions.ts`.
  Notes: `startCheckout` is an action (calls Polar SDK). It creates a Polar checkout with `customer_email`, `success_url: <APP_URL>/settings/billing?status=success`, `cancel_url: <APP_URL>/settings/billing?status=cancel`, `subscription_id` for the plan. Returns `{ checkoutUrl }`. `mine` returns the current user's subscription (or null).

- [ ] **TASK-063** — Implement the Polar webhook httpAction.
  Files: `convex/polarWebhook.ts`, `convex/lib/polarSignature.ts`, `convex/http.ts`.
  Notes: Per `docs/prd.md § 11 → Webhook Handling`. Verify signature, parse event, dispatch on event type. For `subscription.created` and `subscription.updated`: upsert `subscriptions` record by `polarSubscriptionId`. For `subscription.canceled`: update status. For `order.paid` (extra consultation): promote the matching pending booking. Idempotency via `webhookEvents.eventId` unique constraint.

- [ ] **TASK-064** — Wire entitlements into `bookings.create`.
  Files: `convex/bookings.ts`.
  Notes: Per FR-006 + FR-008 + FR-009. Behavior:
  - If user has active subscription with remaining included consultations → `paymentMode: "included_in_subscription"`, decrement counter atomically, create booking immediately.
  - If user has active subscription but 0 included remaining, OR no subscription → `paymentMode: "paid_extra"`, create a Polar order checkout, return `polarCheckoutUrl`. Booking is created in the webhook handler on `order.paid`.
  - If free-tier user with no subscription → only "paid_extra" path; UI prompts to subscribe first.

- [ ] **TASK-065** — Implement `subscriptions.cancelAtPeriodEnd` and `subscriptions.resume`.
  Files: `convex/subscriptions.ts`.
  Notes: Cancel uses Polar SDK's cancellation method with `cancel_at_period_end: true`. Resume undoes it. Both update local state via webhook for consistency, but optimistically update the local `subscriptions` row too.

- [ ] **TASK-066** — Build the `/settings/billing` page.
  Files: `src/app/(app)/settings/billing/page.tsx`, `src/components/billing/SubscriptionCard.tsx`, `src/components/billing/CancelDialog.tsx`.
  Notes: Per `docs/prd.md § 8 → Settings / Billing / Household` and FR-010. Show plan, next renewal, included consultations remaining, cancel/resume controls. Use `useQuery(api.subscriptions.mine)` with reactive updates. On `?status=success`, poll `subscriptions.mine` every 30s until subscription is active (max 2 min) per § 12 → Subscription edge case.

- [ ] **TASK-067** — Update the free-trial paywall and the consultation entry to reflect subscription state.
  Files: `src/app/(app)/consult/page.tsx`, `src/app/consult/new/page.tsx`, `src/components/chat/AnalysisCard.tsx`.
  Notes: Show subscription status hints: "Te queda 1 videoconsulta este mes" / "Has usado tu videoconsulta incluida — la siguiente cuesta 25€." Per Vision § 3 → Product Principles (no dark patterns, urgencia rompe paywall).

- [ ] **TASK-068** — Add the Polar customer portal link to `/settings/billing` for payment method management.
  Files: `convex/subscriptions.ts`, `src/app/(app)/settings/billing/page.tsx`.
  Notes: Action `subscriptions.getCustomerPortalUrl` returns a short-lived URL via Polar SDK. Render as a "Actualizar método de pago" link.

- [ ] **TASK-069** — End-to-end billing test in Polar sandbox mode.
  Notes: With a Polar test card, complete: subscribe monthly, book a video consultation (included), book a second one (€25 paid-extra), cancel subscription, verify state. Document results in `docs/billing-test-runbook.md`.

- [ ] **TASK-070** — Push Phase 4 as a PR.
  Notes: Branch `phase-4/subscription-and-billing`. Title "Phase 4: Subscription & Billing". Wait for review.

-----

## Phase 5: Polish, Trust & Launch Prep

> **Goal:** Tranqui is publicly launchable. Landing page tells the story, legal pages are real, error/empty/loading states are polished everywhere, accessibility is AA-clean, performance hits the targets, analytics + monitoring are wired, and the disclaimer/clinical safety surfaces are audited end-to-end.

**Reference sections:**
- PRD: § 1. Overview > Success Criteria, § 7. Non-Functional Requirements, § 8. UI/UX Requirements > Landing, § 9. Design System, § 12. Edge Cases & Error Handling
- Vision: § 4. Brand Strategy (Positioning, Voice & Tone, Messaging, Elevator Pitches), § 5. Design Direction > Accessibility Commitments

> **Phase prompt:**
> *"Read docs/product-roadmap.md and find Phase 5. Read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. Mark each task complete. After all tasks, push as `phase-5/polish-and-launch-prep` and open a PR for review."*

- [ ] **TASK-071** — Build the marketing landing page `/`.
  Files: `src/app/(marketing)/page.tsx`, `src/components/marketing/Hero.tsx`, `TrustStrip.tsx`, `PricingTeaser.tsx`, `FAQAccordion.tsx`, `Footer.tsx`.
  Notes: Per `docs/prd.md § 8 → Landing`. Copy lifted from Vision § 4 (positioning, headlines, value props, FAQ from objection handlers). Primary CTA "Empezar gratis" → `/consult/new`. Secondary "Iniciar sesión" opens Clerk sign-in modal.

- [ ] **TASK-072** — Build the pricing page `/pricing` and the FAQ page `/faq`.
  Files: `src/app/(marketing)/pricing/page.tsx`, `src/app/(marketing)/faq/page.tsx`.
  Notes: Pricing: one-card layout for Tranqui Mensual €9.99, with "qué incluye", FAQ on extra consultations. FAQ: extracted from Vision § 4 Messaging Framework → Objection handlers, plus 4-5 product-specific FAQs.

- [ ] **TASK-073** — Build legal pages: terms, privacy, disclaimer.
  Files: `src/app/(marketing)/legal/terms/page.tsx`, `legal/privacy/page.tsx`, `legal/disclaimer/page.tsx`.
  Notes: Disclaimer uses `DISCLAIMER_TEXT_LONG`. Terms and privacy: draft template covering: subscription billing terms, refund policy (vet no-show full refund, otherwise no refund after period start), data retention per Open Question Q6, GDPR rights (access, deletion via support email for MVP), governing law (Spain). Have these reviewed by a Spanish legal advisor before launch.

- [ ] **TASK-074** — Polish empty / loading / error states across all main routes.
  Files: across `src/app/(app)/**`, `src/components/shared/EmptyState.tsx`, `src/components/shared/ErrorBoundary.tsx`, `src/components/shared/LoadingSkeleton.tsx`.
  Notes: For each authenticated page (pets, history, bookings, settings), implement: skeleton on first load, friendly empty state with CTA, error toast on mutation failure with retry. Per § 12.

- [ ] **TASK-075** — Add cohesive form validation feedback.
  Files: `src/components/pets/PetForm.tsx`, `src/components/shared/SettingsForm.tsx`, others.
  Notes: All forms use `react-hook-form + zod`; inline errors below each field; aria-describedby for screen readers; submit button shows loading state.

- [ ] **TASK-076** — Accessibility pass.
  Files: across components.
  Notes: Run axe-core in dev, fix all critical issues. Confirm color contrast ratios for cream/sage/coral palette (especially coral on cream). Verify keyboard nav across the consultation flow. Confirm `prefers-reduced-motion` is respected. Verify body text ≥16px on mobile and supports 200% zoom.

- [ ] **TASK-077** — Performance pass.
  Files: across.
  Notes: Run Lighthouse on landing and consultation pages on production preview. Target LCP <2.5s mobile, <1.5s broadband. Reduce JS bundle: dynamic-import the chat page's heavy deps (Daily JS imports lazy on `/bookings/[id]` only). Add `next/font` optimization. Verify Web Vitals show green in Vercel.

- [ ] **TASK-078** — Add Vercel Analytics + Sentry.
  Files: `src/app/layout.tsx`, `sentry.client.config.ts`, `sentry.server.config.ts`, `next.config.mjs`.
  Notes: `pnpm add @vercel/analytics @sentry/nextjs`. Configure Sentry project, add envs, redact PII (no message content). Add `<Analytics />` to root layout.

- [ ] **TASK-079** — Set up SEO basics on marketing pages.
  Files: `src/app/(marketing)/**`, `src/app/sitemap.ts`, `src/app/robots.ts`, `src/app/opengraph-image.tsx`.
  Notes: Per-page `metadata` exports with title, description (Spanish), OG image (use `opengraph-image.tsx` to generate from edge). Sitemap covers landing, pricing, FAQ, legal pages. Robots allows all marketing routes, disallows `/(app)/*` and `/(vet)/*`.

- [ ] **TASK-080** — Final clinical audit pass.
  Notes: Review the next 50 production-style consultations (use a separate test deployment or mark as test runs) with the clinical advisor. Document the appropriate-triage rate and any prompt adjustments needed in `docs/prompt-audit.md`. The bar is ≥85% appropriate triage. This is a quality gate.

- [ ] **TASK-081** — Final disclaimer / safety surface audit.
  Notes: Manually verify (1) disclaimer banner visible at start of every consultation type (free-trial, signed-in, vet-side), (2) disclaimer footer in every analysis card, (3) `/legal/disclaimer` matches the constant, (4) URGENTE bypasses paywall in free-trial post-limit, (5) booking flow does not allow paywall during URGENTE flow. Document the audit in `docs/launch-readiness.md`.

- [ ] **TASK-082** — Domain + production deploy.
  Notes: Configure DNS for `tranqui.app` (or chosen domain) → Vercel. Promote production build. Confirm Convex prod deployment env vars (Anthropic key, Polar live keys, Daily live key, Resend live key, Clerk live keys). Run smoke tests on production: signup, free trial consultation, paid signup with live Polar key (using a personal card; refund), booking with the partner-vet account.

- [ ] **TASK-083** — Push Phase 5 as a PR.
  Notes: Branch `phase-5/polish-and-launch-prep`. Title "Phase 5: Polish, Trust & Launch Prep". Merge only after the launch-readiness checklist passes.

-----

## Phase 6: Post-Launch — Iteration Backlog

> **Goal:** With Tranqui live, prioritize the highest-impact P1/P2 features based on real usage signals. This phase has no fixed completion date — it evolves with the product.

**Reference sections (read on demand per task):**
- PRD: § 6. Functional Requirements > FR-013, FR-018, FR-020, § 14. Out of Scope, § 15. Open Questions
- Vision: § 3. Product Strategy > Feature Priority (MoSCoW), § 2. User Research > Key Assumptions to Validate

> **Phase prompt:**
> *"Read docs/product-roadmap.md and find Phase 6. Pick a single task based on the highest-impact signal from real usage. Read only the relevant PRD/Vision sections per the task's Notes. Implement, push, review."*

- [ ] **TASK-084** — Implement onboarding micro-flow after first signup.
  Files: `src/app/(app)/onboarding/page.tsx`, `src/components/onboarding/Wizard.tsx`.
  Notes: Per FR-020. Three skippable steps: confirm name → add first pet → name your household. Trigger only when `pets.list` is empty and the user has just signed up. Verify: a fresh signup lands here.

- [ ] **TASK-085** — Implement shared household invitations.
  Files: `convex/households.ts`, `src/app/(app)/settings/household/page.tsx`, email template.
  Notes: Per FR-013. Generate a single-use invitation token, send via Resend. Acceptance flow: invitee logs in, lands on `/invite/[token]`, joins the inviting household (their existing household becomes orphaned — handle gracefully).

- [ ] **TASK-086** — Implement pet photo upload.
  Files: `convex/pets.ts`, `src/components/pets/PetForm.tsx`.
  Notes: Per FR-018. Use Convex file storage. Max 2MB. JPEG/PNG/WebP only. Show in pet card and in chat header.

- [ ] **TASK-087** — Build a simple admin/cost dashboard.
  Files: `src/app/(admin)/dashboard/page.tsx`.
  Notes: Aggregate Convex queries: total consultations / week, average cost per consultation, distribution of triage levels, free-tier conversion rate, churn. Admin-only route guarded by `role: "admin"`. Use this view to drive the next priorities.

-----

## Agent Session Guide

### How to Use This Roadmap with Your Coding Agent

1. **Start a session:** Give your coding agent the phase prompt at the beginning of each phase.
2. **Read selectively:** Each phase lists its Reference sections — the specific parts of the PRD and vision doc needed for that phase. The agent should read only those sections, not the entire documents.
3. **Let it work:** The agent reads the roadmap, finds the first unchecked task, implements it, and marks it complete.
4. **One session = one phase (ideally):** Try to complete a full phase in one session for best continuity. If you need to stop, the agent can resume from the last unchecked task.
5. **Push a PR for review:** When a phase is complete, push the work as a PR and let a review agent (e.g. [CodeRabbit](https://coderabbit.ai)) review it before starting the next phase. See the Phase Review section below.
6. **Need more context?** If a task references a section not in the phase's Reference sections, the agent should read just that section on demand.

### Session Tips

- **Don't read everything:** The PRD and vision doc are large. Each phase's Reference sections tell the agent exactly what to read. Loading the full documents wastes context.
- **Don't skip tasks:** Tasks are ordered intentionally. Skipping creates dependency issues.
- **Verify after each phase:** Run the app after completing a phase to confirm everything works before moving on.
- **Review before moving on:** Push a PR for each completed phase and let your review agent check it. Don't start the next phase until the PR is merged. This catches issues early when they're cheap to fix.
- **Update the status line:** After completing tasks, update the header status: `**Status:** X/87 tasks complete` and `**Current Phase:** Phase N`.
- **Use the Convex skills:** The `skills/convex/` directory has specialized guidance (best practices, schema validators, real-time patterns, file storage). Read the relevant SKILL.md when in doubt — `skills/convex/convex-best-practices/SKILL.md` is the most-used.
- **Use the clinical SKILL with care:** `skills/agente-veterinario-ia.md` is the clinical truth source. Do not paraphrase or restructure it inside `convex/lib/prompt.ts` — embed it verbatim.

### Prompt Templates

**Starting a new phase:**
> *"Read docs/product-roadmap.md and find the current phase. Read only the Reference sections listed for that phase from docs/prd.md and docs/product-vision.md. Start working on the first unchecked task. After completing each task, update the checkbox to [x] in the roadmap file. Continue through the phase."*

**Resuming after a break:**
> *"Read docs/product-roadmap.md. Find where we left off (first unchecked task). Read only the Reference sections listed for the current phase from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task."*

**After completing a phase:**
> *"Phase [N] is complete. Create a branch called `phase-{N}/{slug}`, commit all work, push, and open a PR targeting main. Title it 'Phase {N}: {Title}' and include the phase goal and completed task count in the body."*

**Fixing an issue:**
> *"There's a problem with [description]. Read the relevant section of docs/prd.md for the expected behavior and fix it. Don't mark any new tasks complete until the fix is verified."*

### Phase Review Workflow

After every phase, the coding agent pushes a PR (`phase-{N}/{slug}` → `main`) for external review. Recommended review agent: [CodeRabbit](https://coderabbit.ai) — free for open-source, automated review on every PR. The PR body should include:

```
## Goal
{phase goal}

## Completed
- {N} tasks completed (TASK-XXX through TASK-YYY)

## What to verify
- {key behaviors to test}

## Reference sections used
- PRD: § ...
- Vision: § ...
```

If review feedback is raised: address in follow-up commits on the same branch, let the review agent re-review, merge only when clean.
